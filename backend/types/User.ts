export interface User {
    firstName: string;
    userName: string;
    email: string;
    password: string;
}

export interface UserLogin {
    address: string;
    password: string;
    userName: string;
}